# SDK-5 status

- 2026-08-19 — `plainrouter` 0.3.1 published to PyPI through trusted publishing; wheel, sdist, provenance attestations, and a clean-registry install were verified. The PyPI name-exposure window is closed, and the standing availability check is retired.
- 2026-08-19 — `@plainrouter/sdk` and `@plainrouter/cli` 0.3.1 published to npm from annotated tag `v0.3.1`; registry `latest`, the post-publish registry smoke, and a clean SDK registry import were verified. The SDK release chain is closed, and the lane is parked.
