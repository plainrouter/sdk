# plainrouter

Package-name reservation placeholder for the `plainrouter` CLI executable.

Install `@plainrouter/cli` when a supported release is available. This package
contains no runtime or executable.
