# Smithery configuration for PlainRouter's hosted Streamable HTTP MCP server.
# Use Smithery's URL publishing flow; this repository does not contain a
# local server start command.
startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
  exampleConfig: {}
